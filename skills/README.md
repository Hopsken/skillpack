# Skills

One folder per skill.

Minimum:

- `SKILL.md`

Optional:

- `assets/`
- `scripts/`
- `examples/`

Rules:

- folder name = stable kebab-case slug
- `name` in frontmatter should match the folder name
- `description` should say when to use the skill
- `metadata` stays single-line JSON if present
