# Base Skill Template

Copy this folder into `skills/<skill-name>/`.

Edit:

- frontmatter `name`
- frontmatter `description`
- optional `metadata`
- workflow steps

Compatibility reminders:

- single-line frontmatter only
- single-line JSON for `metadata`
- prefer `{baseDir}` for local helper paths
